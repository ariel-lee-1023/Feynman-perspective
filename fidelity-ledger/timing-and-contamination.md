# Evaluation limitations and timing

The legacy runtime core was already read by construction before this evaluation. Its memoir, public-lecture, teaching-review and Challenger material is exposed training data.

The original fixed seed 42 whole-work split assigned that exposed material to final test, so it is invalid for independent release. Files `work-inventory.json` and `work-split.json` preserve that failure. No seed search was done.

The corrective split reserves complete previously-unread Six Easy Pieces lecture episodes. Chapters 1–3 form an eligible inventory; seed 42 assigns chapter 2 to final, chapter 1 to development, and chapter 3 to training. Chapters 4–6 are training because of overlap risk with legacy examples. The five exposed works and construction-recovered Tips are deterministically forced training. This exposure-constrained episode split is documented, not described as a successful unconditioned whole-work split.

Construction reports the source split froze before new trait reads. The evaluator privately read reserved evidence; construction has not received target passages, expected answers or final prompts. Broad latent pretrained familiarity and exact cross-work retellings cannot be mechanically excluded. Technical projection has only one lecture group per held-out phase, with two correlated probes. It measures limited physics pedagogy coverage.

The behavioral suite was frozen AFTER construction began reading BBC and Tips teaching/sign reconstruction for traits. Therefore `defined_before_extraction` is false. The executable runner’s pre-extraction task contract is unmet, even though final prompts and targets remain withheld. Do not claim a verified release from this suite. The source-split-before-extraction declaration applies to source projection, not retroactively to behavioral suite timing.

Before any prediction, a revision replaced one final scope scenario that shared a wartime scenario with development. The historical first freeze and hashes remain under `initial-freeze/`. The revised suite keeps engineering-related final scenario variants grouped together. No predictions or scores existed before either freeze.

Current purpose: honest manual development comparison and carefully qualified reserved observations on stable candidate bytes. No predictions, grading, or fabricated scores have been produced by this custodian.
