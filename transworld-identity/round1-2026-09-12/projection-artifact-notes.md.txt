Projection fragment preserves required fields and exact split item IDs. Fresh condition-level batches do not establish strict per-item fresh context, so fresh_context=false and passed=false are retained. This intentionally fails the schema const:true instead of laundering manual observations into verified evidence. Numerical candidate/baseline scores and original result hashes remain correct.
